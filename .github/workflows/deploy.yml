name: Deploy Microservice

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout the repository
      - name: Checkout repository
        uses: actions/checkout@v2

      # Step 2: Connect via SSH and copy files
      - name: Connect via SSH and copy files
        run: |
          scp -i "${{ secrets.AWS_EC2_SSH_KEY }}" -r ./* ubuntu@${{ secrets.AWS_EC2_IP }}:/node-app/microservice-template
          ssh -i "${{ secrets.AWS_EC2_SSH_KEY }}" ubuntu@${{ secrets.AWS_EC2_IP }} << EOF
          cd /node-app/microservice-template
          docker compose --profile deploy up --build -d
          EOF
